# 律途人生（LegalLife）项目设计及开发文档

> 版本：v1.0.1 ｜ 更新日期：2026-08-19

---

## 一、项目概述

**律途人生**是一款融合真实法律科普的人生模拟游戏。玩家创建角色后，从童年一路体验至老年，每个生活场景都关联真实法律条文。玩家做出的每个选择，都会导向不同的人生分支，并解锁对应的法律知识。

### 核心理念

> **每一次选择，都是一堂法律课。**

- **沉浸式人生模拟**：文字剧情 + 场景配图，体验类《人生重开模拟器》的玩法
- **真实法律科普**：每个场景关联真实法条，含「法条原文 + 通俗解读 + 来源链接」
- **跨国法律对比**：同一人生阶段在不同国家（中/美/欧）有不同法律场景
- **分支剧情**：积极/合法的选择导向人生主线，消极/妥协的选择随波逐流

### 当前完成度

约 93%。核心可玩闭环完整，已交付 Android APK（v1.0.x）。

---

## 二、技术架构

### 技术栈

| 层 | 技术 |
|---|---|
| 移动端 | React Native 0.86.2 + Expo SDK 57 + Expo Router |
| 后端 | Supabase（PostgreSQL + Storage + Edge Functions）|
| 状态管理 | Zustand v5 |
| 国际化 | i18next（中英双语）|
| 管理后台 | Next.js 15 |
| 语言 | TypeScript 全栈 |
| 工程化 | pnpm monorepo（Turborepo）|

### 目录结构

```
legal-life/
├── apps/
│   ├── mobile/              # 移动端（Expo + React Native）
│   │   ├── app/             # 页面（expo-router 路由）
│   │   │   ├── (tabs)/      # 首页/图鉴/个人等标签页
│   │   │   └── game/        # 角色创建 + 场景玩法页
│   │   ├── lib/             # 共享库、API 层、场景触发算法
│   │   ├── stores/          # Zustand 状态
│   │   └── assets/images/   # 打包进 App 的场景图片
│   └── admin/               # Next.js 管理后台
├── packages/
│   ├── shared/              # 共享类型/常量（monorepo 内）
│   └── game-engine/         # 游戏引擎（场景触发/年龄推进等）
├── legal-data/
│   ├── cn/  us/  eu/        # 按国家组织的法条(national)与场景(scenes)数据
│   └── scripts/             # 数据导入脚本
├── scripts/                 # 图片压缩/打包/上传脚本
└── supabase/
    ├── migrations/          # 数据库迁移（001/002/003）
    └── functions/           # Edge Functions（图片生成）
```

### 数据流

```
角色创建 → 选国家(CN/US/EU) → 加载该国场景(按年龄+未完成过滤)
  → 加权选场景(难度匹配+类别多样性) → 展示叙述+配图(本地资源)
  → 选择分支(下集预告/硬分支链) → 后果+法条原文解读 → 收集法条 → 年龄推进
```

---

## 三、数据库设计

Supabase PostgreSQL，15 张表，核心内容表 4 张：

| 表 | 说明 |
|---|---|
| `countries` / `regions` | 国家（CN/US/EU…）与地区 |
| `law_categories` | 法律分类（8 类）|
| `laws` | 法条（112 条，含多语言原文/解读/来源）|
| `life_stages` | 人生阶段（6 个）|
| `scenarios` | 游戏场景（41 个，含 key/年龄范围/难度）|
| `scenario_choices` | 场景选项（含分支标签/下集预告/硬分支引用）|
| `scenario_laws` | 场景-法条关联 |
| `game_saves` / `play_history` / `law_collections` | 存档/游玩记录/法条收集 |
| `achievements` / `user_achievements` | 成就系统 |
| `image_cache` | AI 图片缓存（预留）|

**RLS 策略**：内容表（laws/scenarios 等）公开可读；存档/收集按 `auth.uid()` 隔离（当前未启用登录，纯本地）。

**关键迁移**：
- `001_initial_schema.sql` — 完整表结构
- `002_functions_and_indexes.sql` — 函数与索引
- `003_scenario_chain.sql` — 场景连贯性（scenarios.key + choices 的 branch_tag/next_scene_hint/next_scene_key）

---

## 四、核心功能设计

### 4.1 场景连贯性（混合方案）

「选择导向不同人生分支」是本项目的核心理念，实现分两层：

- **软性连贯**：结果页展示「下集预告」（`next_scene_hint`）+ 记录玩家分支倾向（`branchTagHistory`）
- **硬分支链**：选项的 `next_scene_key` 显式指向下一场景，形成确定性链条

**四条分支链**：
| 链 | 走向 |
|---|---|
| 职场线 | 入职 → 加班 → 辞退 |
| 消费线 | 网购维权 → 健身房跑路 |
| 民事线 | 租房押金 → 婚前买房 |
| 交通线 | 酒驾 → 剐蹭 → 电动车 |

设计逻辑：**积极/合法选择导向主线，消极/妥协选择走加权随机**（对应「积极选择有主线、消极选择随波逐流」）。

### 4.2 加权场景选择

内联 `lib/scenarioTrigger.ts`（移植 game-engine 的 `selectScenario`），替代纯随机：
- 难度匹配加权（越接近中等难度权重越高）
- 类别多样性降权（最近玩过的同类场景降权）

### 4.3 本地持久化

`gameStore` 用 zustand `persist` + AsyncStorage，`partialize` 只持久化长期状态（角色/收集/分支链/存档），排除瞬时 UI 态。重启不丢档。

### 4.4 多国支持

- 数据按国家目录组织：`legal-data/{cn,us,eu}/`
- 导入脚本遍历多国目录，自动新增/激活国家
- 前端角色创建可选国家，场景/图鉴按角色国家过滤

### 4.5 场景图片系统（三层演进）

1. **云端加载**：图片上传 Supabase Storage，App 通过 URL 加载
2. **压缩优化**：`compress-images.mjs` 将 3.2MB 的 PNG 缩到 65KB 的 JPEG（节省 98%）
3. **本地化打包**（最终方案）：`build-scene-images.mjs` 将图片打包进 App，`sceneImages.ts` 生成 require 映射，`scenario.tsx` 本地图优先、云端 URL 兜底——彻底消除网络延迟

---

## 五、数据设计

### 5.1 法条数据（112 条）

| 国家/地区 | 法条数 | 覆盖领域 |
|---|---|---|
| 🇨🇳 中国 | 82 | 民法典(20)/刑法(14)/劳动法(14)/消保法(10)/未成年人保护(8)/交通(8)/个人信息(8) |
| 🇺🇸 美国 | 16 | FLSA 劳动/FTC 消费/租赁侵权/酒驾/刑事 |
| 🇪🇺 欧盟 | 14 | GDPR（同意/被遗忘权/可携带/泄露通知/罚款）|

每条法条含：`code`（唯一编号）、法条原文、通俗解读、关键词、标签、来源链接。

### 5.2 场景数据（41 个）

覆盖全部 6 个人生阶段 × 3 国家：

| 人生阶段 | 中国 | 美国 | 欧盟 |
|---|---|---|---|
| 童年(6-12) | 4 | - | - |
| 少年(13-17) | 4 | 1 | - |
| 青年(18-25) | 9 | 4 | 4 |
| 壮年(26-40) | 6 | 1 | 1 |
| 中年(41-60) | 3 | - | - |
| 老年(61+) | 4 | - | - |

### 5.3 法条编号体系

- 格式：`{国家代码}-{分类缩写}-{三位序号}`，如 `CN-LABOR-001`、`US-TRAFFIC-001`、`EU-CYBER-006`
- 分类缩写：employment→LABOR、consumer_rights→CONSUMER、traffic→TRAFFIC、criminal→CRIM、civil→CIVIL、education→EDU、cyber_security→CYBER
- 场景的 `laws_revealed` 直接引用法条 code（2026-08-18 已清理历史错位编号，删除了 REVEAL_MAP 机制）

---

## 六、开发过程记录

按本次开发的时间线：

1. **场景连贯性（混合方案）**
   - 新增 003 迁移：`scenarios.key` + `choices.branch_tag/next_scene_hint/next_scene_key`
   - 职场线硬分支链示范，结果页「下集预告」卡片

2. **本地持久化 + 同步架构**
   - zustand persist + AsyncStorage
   - `performSync` 填真实骨架（登录检查 + 队列管理 + 注入式同步）

3. **加权选择接入**
   - 内联 `lib/scenarioTrigger.ts`（规避 monorepo Metro 解析坑）

4. **法条扩充 29 → 112 条**
   - 补齐 4 个悬空引用（加班/预付卡/婚前财产/租赁押金）
   - 新增美国 16 条、欧盟 GDPR 14 条

5. **场景扩充 9 → 41 个**
   - 补齐 childhood/middle_age/senior 空白阶段
   - 新增 teen/traffic 线串联、美国/欧盟场景

6. **编号体系清理**
   - 删除 REVEAL_MAP 错位机制，场景引用直接对应 code

7. **场景图片接入**
   - 上传 Supabase Storage → 压缩 98% → 打包进 App（本地化）

8. **EAS 打包 Android APK**
   - 配置 eas.json、环境变量，交付 v1.0.0 / v1.0.1

---

## 七、遇到的问题与解决方案

| # | 问题 | 根因 | 解决方案 |
|---|---|---|---|
| 1 | 法条编号错位/悬空 | 场景 JSON 的旧编号与 yaml 生成编号不一致 | 补齐悬空法条 + 清理错位，删除 REVEAL_MAP 机制 |
| 2 | Metro 白屏/500 | `metro.config.js` 的 `sourceExts` 覆盖而非追加，丢了 css | 改为「追加」方式 |
| 3 | 字段不匹配 | Supabase 返回 snake_case，前端用 camelCase | gameApi 内建 snakeToCamel/camelToSnake 递归转换 |
| 4 | DDL 无法走 REST | REST 只能 DML，不能 ALTER TABLE | 迁移 SQL 由用户在 SQL Editor 手动执行 |
| 5 | **APK 黑屏** | `.env` 在 .gitignore，EAS 构建不上传 → Supabase 配置为空 | 用 `eas env:set` 把环境变量配置到 EAS 云端 |
| 6 | **图片加载慢** | 图片在海外 Supabase Storage，国内访问延迟高 | 压缩 98% + 打包进 App（本地化）|
| 7 | JSON 解析报错 | 美国场景中文文本误用英文双引号 | 改用中文引号「」 |
| 8 | EAS 构建报错 | 项目未关联 EAS | 先 `eas init` 创建项目 |
| 9 | eas login 超时 | 浏览器授权耗时超过会话命令 120s | 建议在自己终端跑登录 |

---

## 八、部署与打包

### EAS Build 打包 Android APK

```bash
# 首次：登录 + 关联项目
eas login
eas init --account shenluonanshu

# 配置环境变量（关键，否则 APK 黑屏）
eas env:set --name EXPO_PUBLIC_SUPABASE_URL --value "https://xxx.supabase.co" --environment preview --visibility plaintext
eas env:set --name EXPO_PUBLIC_SUPABASE_ANON_KEY --value "xxx" --environment preview --visibility plaintext

# 构建 APK
eas build -p android --profile preview
```

- EAS 项目 ID：`0dabcc71-d717-4123-986f-d3491e672430`
- `eas.json` 的 preview profile 配 `"android": {"buildType": "apk"}` 打 APK（默认 AAB）
- 图片打包：`node scripts/compress-images.mjs && node scripts/build-scene-images.mjs`（构建前执行）

### 数据导入

```bash
SUPABASE_URL=xxx SUPABASE_SERVICE_KEY=xxx node legal-data/scripts/import-to-supabase.mjs [--dry-run] [--country CN,US,EU]
```

---

## 九、后续规划

### 待完成
- [ ] 日本/韩国法律数据（Phase 3-4）
- [ ] App 图标美化（当前为 Expo 默认图标）
- [ ] App Store / Google Play 上架

### 已决定不做（2026-08-18）
- 登录功能 + 云同步端到端（纯本地单机体验，本地 persist 已够用）
- Replicate API 自动生成图片（改用用户手动生成 + 脚本接入）

---

## 附：关键文件索引

| 文件 | 说明 |
|---|---|
| `supabase/migrations/*.sql` | 数据库迁移（001 建表 / 003 场景连贯性）|
| `apps/mobile/stores/gameStore.ts` | Zustand 状态管理（分支链 + persist）|
| `apps/mobile/app/game/scenario.tsx` | 核心游戏循环页面 |
| `apps/mobile/lib/api/gameApi.ts` | Supabase 服务层（字段映射）|
| `apps/mobile/lib/scenarioTrigger.ts` | 加权场景选择（内联）|
| `apps/mobile/lib/sceneImages.ts` | 场景图片 require 映射（自动生成）|
| `legal-data/{cn,us,eu}/` | 多国法条 + 场景数据源 |
| `scripts/compress-images.mjs` | 图片压缩（PNG→JPEG，省 98%）|
| `scripts/build-scene-images.mjs` | 图片打包进 App |
| `scripts/upload-scenario-images.mjs` | 图片上传 Storage + 关联场景 |
